[build]
  command = "npm run build"
  publish = ".next"
  environment = { NODE_VERSION = "18" }

[functions]
  node_bundler = "esbuild"
  external_node_modules = [
    "sharp",
    "moment",
    "pdfkit",
    "aws-sdk",
    "@aws-sdk/*",
    "puppeteer-core"
  ]
  included_files = [
    "./public/**/*",
    "./next.config.js"
  ]

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Optional: Redirects for client-side routing fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
